// Objetos: los objetos son construcciones complejas que nos permite almacenar dentro distintos elementos como 
// numeros, booleanos, strings, otros objetos, etc., es decir, basicamente, informacion o datos sobre una cosa.

// Las propiedades de un objeto son, precisamente, esos datos/informacion que queremos agregarle para que de forma a ese objeto.
// Se denominan tambien "clave", a las cuales se les asigna un valor, que vendría a ser el contenido de ese dato/informacion.

// Se denomina método a una funcion cuando se encuentra dentro de un objeto. Este método hace algo, ademas de contener datos
// o información, nos devuelve lo que le pedimos que haga con el objeto o con algunas de sus propiedades. 

//La notacion de puntos o de corchetes son las dos formas de acceder a las propiedades de un objeto. La notacion de puntos,
// tambien llamada DOT notation, se utiliza cuando conocemos el nombre exacto de la propiedad ya que es literal. Al igual que
// la notacion de corchetes utilizando comillas. En cambio, la notacion de corchetes o brackets, se utiliza cuando no conocemos
// con exactitud el nombre de la propiedad, sino que nos fue asignada por argumento por ejemplo. 

//Los bucles for... in sirven para recorrer los objetos y acceder a sus propiedades. Como los objetos no tienen un 
// indice numerico, sino que son claves. Por lo tanto funciona un poco distinto al bucle de los arrays por ejemplo.
// Debemos escribir for y, entre parentesis una variable, que será la clave, seguido de in y el objeto que queremos recorrer.
// Tambien aplican aqui las dos notaciones, segun conozcamos o no el nombre de la clave o propiedad a la cual queremos acceder.